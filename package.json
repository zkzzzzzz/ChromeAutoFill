{
  "name": "smartfill-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that autofills form fields (e.g. security questions) by keyword or CSS selector rules.",
  "scripts": {
    "test": "node tests/run.js"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
